package com.atos.task.repository;

import com.atos.task.model.AccessPort;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AccessPortRepository extends JpaRepository<AccessPort,Long> {
}
